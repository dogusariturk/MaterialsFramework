# EqNorm

!!! info "Optional dependency"

    `EqnormCalculator` requires the `eqnorm` extra.

    === "uv"

        ```bash
        uv add "materialsframework[eqnorm]"
        ```

    === "pip"

        ```bash
        pip install "materialsframework[eqnorm]"
        ```

::: materialsframework.calculators.eqnorm.EqnormCalculator
