# EquiformerV2

!!! info "Optional dependency"

    `EqV2Calculator` requires the `eqv2` extra.

    === "uv"

        ```bash
        uv add "materialsframework[eqv2]"
        ```

    === "pip"

        ```bash
        pip install "materialsframework[eqv2]"
        ```

::: materialsframework.calculators.eqv2.EqV2Calculator
