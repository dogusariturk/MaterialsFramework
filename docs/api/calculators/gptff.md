# GPTFF

!!! info "Optional dependency"

    `GPTFFCalculator` has no `materialsframework` extra. It's only installable from its upstream git repository. See [Non-Extra Calculators](../../installation.md#non-extra-calculators) for details.

    === "uv"

        ```bash
        uv pip install "gptff @ git+https://github.com/atomly-materials-research-lab/GPTFF.git"
        ```

    === "pip"

        ```bash
        pip install "gptff @ git+https://github.com/atomly-materials-research-lab/GPTFF.git"
        ```

::: materialsframework.calculators.gptff.GPTFFCalculator
