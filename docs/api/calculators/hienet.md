# HIENet

!!! info "Optional dependency"

    `HIENetCalculator` requires the `hienet` extra.

    === "uv"

        ```bash
        uv add "materialsframework[hienet]"
        ```

    === "pip"

        ```bash
        pip install "materialsframework[hienet]"
        ```

::: materialsframework.calculators.hienet.HIENetCalculator
