# GRACE

!!! info "Optional dependency"

    `GraceCalculator` requires the `grace` extra.

    === "uv"

        ```bash
        uv add "materialsframework[grace]"
        ```

    === "pip"

        ```bash
        pip install "materialsframework[grace]"
        ```

::: materialsframework.calculators.grace.GraceCalculator
