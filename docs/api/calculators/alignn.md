# ALIGNN

!!! info "Optional dependency"

    `AlignnCalculator` requires the `alignn` extra.

    === "uv"

        ```bash
        uv add "materialsframework[alignn]"
        ```

    === "pip"

        ```bash
        pip install "materialsframework[alignn]"
        ```

::: materialsframework.calculators.alignn.AlignnCalculator
