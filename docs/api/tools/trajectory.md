# TrajectoryObserver

`relax()` and `run()` construct and attach a `TrajectoryObserver` internally. It is not meant to be constructed or attached directly. See the [usage guide](../../usage/tools/trajectory.md) for how to read trajectory data back from their results.

::: materialsframework.tools.trajectory.TrajectoryObserver
